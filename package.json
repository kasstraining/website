{
  "name": "kass-website",
  "version": "1.0.0",
  "description": "Marketing site for kass.training — built locally, served by GitHub Pages from docs/",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
